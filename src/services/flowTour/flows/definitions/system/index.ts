import { FlowDefinition } from "../../../interface";
import { systemOverviewFlow } from "./systemOverview";

// Fluxos gerais do sistema (disponíveis no Dashboard).
export const systemFlows: FlowDefinition[] = [systemOverviewFlow];
