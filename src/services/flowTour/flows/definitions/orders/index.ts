import { FlowDefinition } from "../../../interface";
import { ordersFlow } from "./orders";

// Fluxos da rota /orders, na ordem de exibição.
export const ordersFlows: FlowDefinition[] = [ordersFlow];
