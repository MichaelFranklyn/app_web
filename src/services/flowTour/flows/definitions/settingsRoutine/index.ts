import { FlowDefinition } from "../../../interface";
import { settingsRoutineFlow } from "./settingsRoutine";

// Fluxos da rota /settings/routine.
export const settingsRoutineFlows: FlowDefinition[] = [settingsRoutineFlow];
