import { FlowDefinition } from "../../../interface";
import { clientsFlow } from "./clients";

// Fluxos da rota /clients, na ordem de exibição.
export const clientsFlows: FlowDefinition[] = [clientsFlow];
