import { FlowDefinition } from "../../../interface";
import { factoriesFlow } from "./factories";

// Fluxos da rota /factories, na ordem de exibição.
export const factoriesFlows: FlowDefinition[] = [factoriesFlow];
