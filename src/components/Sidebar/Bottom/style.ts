import { cn } from "@/lib/utils";

export const sidebarBottomStyles = {
  root: cn(
    "mt-auto flex items-center gap-[10px] border-t border-(--border) px-[14px] py-[10px]"
  ),
  name: "truncate leading-tight",
  role: "truncate leading-tight",
};
