import { CardBg } from "../interface";
import { bgVariants, cardFooterBaseStyle } from "../style";

export const footerStyle = cardFooterBaseStyle;

export const footerBgStyle: Record<CardBg, string> = bgVariants;
