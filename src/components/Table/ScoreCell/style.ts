export type ScoreColor = "red" | "orange" | "amber" | "blue" | "green";

export const SCORE_COLOR_MAP: Record<ScoreColor, string> = {
  red: "var(--red)",
  orange: "var(--orange)",
  amber: "var(--amber)",
  blue: "var(--blue)",
  green: "var(--green)",
};
