export type ScoreColor = "red" | "amber" | "blue" | "green";

export const SCORE_COLOR_MAP: Record<ScoreColor, string> = {
  red: "var(--red)",
  amber: "var(--amber)",
  blue: "var(--blue)",
  green: "var(--green)",
};
