// Máquina de estágios (full/icon/column) das ações de header, compartilhada
// entre `Card.Header`/`Table.CardHead` (ações ao lado do título) e
// `PanelHeader.Actions` (ações em linha própria, abaixo do título).
export { HeaderActionsGroup, useHeaderActionsGroup } from "./Group";
export type { HeaderActionsNeed } from "./Group";
export { useHeaderActionsMode } from "./useHeaderActionsMode";
export type { HeaderActionsMode } from "./useHeaderActionsMode";
