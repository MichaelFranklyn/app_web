import ProductsContent from "./content";

/**
 * Análise de compra por produto. Sem gate de plano: sai dos pedidos, não do
 * motor de rotina — quem só vende e fatura precisa dela igual.
 */
const Page = () => <ProductsContent />;

export default Page;
